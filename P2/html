<div class="item">
    <img class="fondo" src="${objeto.img}">
    <div class="content">
        <div class="name">${objeto.category}</div>
        <div class="title">${objeto.name}</div>
        <div class="topic">${objeto.descripcion} </div>
        <div class="des">
            <h1>${objeto.caracteristics[0]}</h1>
               ${objeto.caracteristics[1]}
        </div>
        <div class="buttons">
            <button class=replaceClass id="productButton" onclick="addToCart(${objeto.id})"> ADD 🛒 </button>
        </div>
    </div>
</div>